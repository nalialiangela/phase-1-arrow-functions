const divide = function(array, divisor)
{
    return array.map(item => item / divisor);

}

const resul =divide([2000], 100);
console.log(resul);


const square = (number) => number * number;
const num = 5;
const squaredNum = square(num);
console.log(squaredNum);

const add = (a, b) => a + b;
const result = add(5,3);

console.log(result);